let fruits = {
    molla: "kuqe",
    banane: "verdh",
    kumbull: "jeshile",
}

let keys = Object.keys(fruits)

for (let i = 0; i < keys.length; i++) {
    let fruta = keys[i]
    console.log("fruta " + fruta + " ngjyra " + fruits[fruta])
}
